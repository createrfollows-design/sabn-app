# SABN Frontend

React + TypeScript Mini App находится в `src/App.tsx`. При запуске внутри Telegram приложение вызывает `Telegram.WebApp.ready()`, `expand()` и `requestFullscreen()` для Full Size режима.